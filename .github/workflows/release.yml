name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  python-distributions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      - run: python -m pip install --upgrade pip build
      - run: python -m pip install -e .
      - run: python -m unittest discover -s tests -v
      - run: python -m build
      - uses: actions/upload-artifact@v6
        with:
          name: python-distributions
          path: dist/
      - run: python -m pip install --force-reinstall --no-deps dist/*.whl
      - run: ag2c --version

  windows-installer:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      - name: Install installer build tools
        shell: pwsh
        run: |
          python -m pip install --upgrade pip pyinstaller
          if (-not (Get-Command iscc.exe -ErrorAction SilentlyContinue)) {
            choco install innosetup --no-progress -y
          }
      - name: Build and smoke-test Windows installer
        shell: pwsh
        run: |
          $version = $env:GITHUB_REF_NAME.TrimStart('v')
          ./scripts/build_windows_installer.ps1 -Version $version
          ./scripts/test_windows_installer.ps1 `
            -InstallerPath build/windows/installer/AutoGovern2Code-Setup-Windows-x64.exe `
            -Version $version
      - uses: actions/upload-artifact@v6
        with:
          name: windows-installer
          path: build/windows/installer/AutoGovern2Code-Setup-Windows-x64.exe

  release:
    needs: [python-distributions, windows-installer]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v6
        with:
          path: release-assets
          merge-multiple: true
      - name: Create checksums
        run: sha256sum release-assets/* > release-assets/SHA256SUMS.txt
      - name: Create GitHub Release
        run: gh release create "$GITHUB_REF_NAME" release-assets/* --verify-tag --generate-notes --title "AutoGovern2Code $GITHUB_REF_NAME"
        env:
          GH_TOKEN: ${{ github.token }}
