# Entry slicing

Withdrawn as a standalone maintainer page.

Routing still happens: `ag2c task start --path` / `--contract` / `--all`, then
`task verify` recompiles from the actual diff. Ordinary use is the Skill, not
this file.

See [Automatic governance](AUTOMATIC_GOVERNANCE.md) and
[Architecture](ARCHITECTURE.md).
