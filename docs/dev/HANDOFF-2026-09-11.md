# 交接文档 2026-09-11（临时）

> **性质**：临时文档。归档条件：当下列「已决策的下一步」全部闭环、或本文档年龄超过两周且被新交接文档取代时，由继任任务删除或改写。接手方式：新会话先 `ag2c_task_orient`（MCP）拿开放任务队列，再读本文件，再查账本尾部事件。

## 1. 当前状态

- main @ `fe93cd0`（本机领先 origin）；v0.10.0 已发布（tag 在 `c2e4c5b`）。AGF 试飞报告已入账。
- `src/` 没有超过 800 行的 Python 文件（最大 `imgui_tray.py` 792）。11 个原胖文件已 flatten-split 收口：`ec6915b` tasks.py → `1fa289a` gitops.py → `d3ea24a` graph.py → `225d924` households.py → `18306db` govern.py → `975f78e` checks.py → `8b8046a` tray_host.py → `253fa8e` enrollment.py → `168dc12` mcp_server.py → `7bfbf96` cli.py → `b21c8bd` imgui_tray.py（AppState 留守）。
- 反向开发机制（早→晚）：`cb2602e` flatten-queue + flatten-check → `af18454` portrait.py → `2203f1a` tests 减重 → `052febf` flatten-split 原语 → `25ee745` task_evidence.py → 上列 11 文件摊平。
- 更早相关：`91158e4` 降档棘轮 → `aba1feb` scenario always=true → `d6c3136` MCP census 观察默认摘要。
- AGF 对账三件套：坐标申报 ✅（fbf13b0）／对账 ✅（f62fb97）／降档棘轮 ✅（`91158e4`）。
- 审计四发现全部闭环：strict 401 泄漏（fa07796）、hazard 只拉不推（285ac0b）、notify 空炮（285ac0b）、patrol 只在 finish（托盘轮询即 watchdog）。
- 治理面：39 房间普查全 current；settle 清零；账本 3005 事件。
- 影子数据判决：精确 digest 缓存命中率 0%（16 次 verify），粗相关性回放可省 7.5% 且 0 漏失败——**Phase 2 引擎不做**，影子模式继续白跑攒数据。

## 2. 已决策的下一步（按序）

1. ~~剪贴板防御~~ ✅ `714cb18`（另含 MCP 版本自检+普查瘦身；ClipboardLock.held 口径精化：仅在本进程打不开剪贴板时为真）。
2. ~~成本治理第一期~~ ✅ `6bba67e`（监管 3 次驳回后过——严格模式实战立功；数据开始积累，棘轮阈值 2-4 周后定）。
3. ~~AGF 入学试飞~~ ✅ 2026-09-12，项目 `C:\WorkSpace\Code\agf`（store `agf-e883cbdd1033`）。入学已在 2026-09-10；本次补 first-drill canary、冷启动建图、最小任务 `3559a62`。报告见下。
4. ~~反向开发机制 + 11 个胖文件摊平~~ ✅ 机制 `cb2602e`/`052febf`；首航 `af18454`/`25ee745`；11 文件收口 `b21c8bd`（`src/` 没有超过 800 行）。
5. **托管模式/市长代理层**——~~决策分类学~~ ✅ 2026-09-12（表见下）。**代理规则引擎不做**；抽查回火不开工。可逆周界（settle／census 认领／警告认领）可进 L0 提案；finish 合并／删除留人。
6. ~~降档棘轮（三件套收官）~~ ✅ `91158e4`。

### 托管模式（市长代理层）——2026-09-11 晚与市长定稿

**定义**：托管模式 = 市长不看「需要你处理」，看「代理正在做什么」。仪表盘重心从决策队列翻转为代理活动流。市长的动词只剩三个：**看**（代理活动流）、**否**（否决窗口）、**授**（授予新代理权）。

**现状**：显示层已就绪（注意力闸门 t42／三区 t51／颜色纪律 t52／警情必达 285ac0b／托盘双心跳）；**代理层空白**——2026-09-11 实战市长被摸 6-8 次，仅约 2 次真需人判（杀 QQ、预算口径裁决），其余是机械事项走人工通道（settle 清零、full-scan 确认、已知良性警告认领）。

**三块建设**：

1. ~~决策分类学~~ ✅ 2026-09-12，表见下。
2. **代理规则引擎**——**代理规则引擎不做**（本任务）。每个自动决定落账并带授权规则 id，可审计可撤回；代理权的授予记录本身进账本（代理权是市长授予的，不是系统自取的）。
3. **抽查回火**——不开工。代理决定进随机抽查池（接 t911 既有机器）——代理权不被审计就会变成篡权。

**信任阶梯合流**：代理权按 AG2K 梯子爬——L0 提案规则（默认关闭）→ L1 机器锚定（规则落 policy）→ L2 实战存活（N 次代理决定零否决）→ L3 常备授权。**托管模式是 AG2K 从抽象概念变成日常操作的地方**。

**安全序**：代理权先覆盖可逆周界（settle／普查／警告认领）；不可逆动作（合并／删除）永远留人。

**北极星**：市长介入次数/天 → 0-1。挂成本治理「高效」腿（6bba67e 的仪表已预留位置）。

### 决策分类学（2026-09-12，试飞校准）

**口径**：点位 = 今天会进看板「需要你处理」、托盘 P0 通知、或 finish 门禁的事项。判定只有两档：**规则可决**（可逆，L0 提案候选）／**必须人决**（不可逆或价值/宪法）。本表不授权自动执行——**代理规则引擎不做**。分组借用 PROXY-GOVERNANCE §6 四档。

**规则可决**（可逆周界，L0 先覆盖）：

| 点位 | 现状通道 | 判定 | 校准依据 |
|---|---|---|---|
| settle：stale-knowledge / new-document / changed-document | finish `governance_pending` → 看板待结算 | 规则可决 | 2026-09-11 机械通道；settle 同步不发明 |
| census 认领（已检视房间的 census-review-required / census-stale） | pending + notify `census-stale` | 规则可决 | 试飞#9 settle 不代录普查会再摸市长；安全序点名普查 |
| 警告认领（file-soft-cap、coordinate-reconciliation 未满 3 次） | warning-history | 规则可决 | 2026-09-11「已知良性警告认领」走人工 |
| 冷启动 full-scan 确认 | verify 拒 stale | 规则可决 | 试飞#5；agent 已检视则记录 |
| 分叉 worktree → `task refresh` | 看板 diverged | 规则可决 | 机械同步；abandon 除外 |

**必须人决**（不可逆 / 价值 / 宪法；判断类暂入此档，不进 L0）：

| 点位 | 现状通道 | 判定 | 校准依据 |
|---|---|---|---|
| finish 合并 | `task finish` fast-forward | 必须人决 | 安全序；试飞 backlog「不要覆盖 finish 合并」 |
| 删除 / `retire-confirm` | govern retire | 必须人决 | 安全序 |
| `task abandon` | 丢弃 worktree | 必须人决 | 丢工作不可逆 |
| guard-removed | hazard P0（severity 60） | 必须人决 | 宪法：守卫拆除 |
| regulator-absent（已配置但不回答；非 not-configured） | hazard P0（50） | 必须人决 | 语义层失明 |
| `regulator --strict off` | govern regulator | 必须人决 | 降级承认 |
| ungoverned-commit | hazard（45，不自动推） | 必须人决 | 市长直推也要认领 |
| assertion-conflict | pending | 必须人决 | 不能静默覆盖要点 |
| opaque-household / fake-child / tighten-or-renew | pending | 必须人决 | 房间身份 |
| unowned-area / new-interface | pending | 必须人决 | 价值类新地盘/新契约 |
| undeclared-product 首次申报 | pending | 必须人决 | 试飞#7；settle 不发明产品检查 |
| over-budget 第三次锁死 / 抬房间预算 | ESCALATABLE / policy | 必须人决 | 2026-09-11 预算口径裁决 |
| 抽查到期 | 看板 audit | 必须人决 | 抽查就是人；抽查回火不开工 |
| 杀占用核的外部进程 | 无产品通道 | 必须人决 | 2026-09-11 杀 QQ |
| 外项目监管缺省（not-configured） | verify unavailable | 必须人决（每项目一次） | 试飞#3 |
| warning-dismiss / hazard-dismiss 带日落 | 豁免命令 | 必须人决 | PROXY 判断类；豁免是政策 |
| possible-duplicate 第三次 | ESCALATABLE | 必须人决 | 合并重复是设计 |
| 宪法卡 / 5.1 / GOVERNANCE_CODE_PATHS | 设计对话 | 必须人决 | PROXY 宪法类；5.1 勿直接委托 |

**不摸市长**（产品/agent 缺陷，不进注意力闸门）：试飞#1 MCP 必须显式 cwd、#2 PowerShell `$env:PYTHONPATH`、#4 enroll 不设 git identity、#6 canary 中文乱码、#8 first-drill 入学后没人跑。修在产品，不进代理权。

**L0 覆盖顺序**（仍不执行）：settle → census 认领 → 警告认领。合并／删除／abandon 永不进 L0。

### AGF 试飞报告（2026-09-12）

**项目**：`C:\WorkSpace\Code\agf`，七维策略预设骨架，v0.2.0，5 个代码文件。入学 2026-09-10（本会话不是从 enroll 开始）。

**冷启动建图**：enrollment 只挂 exploring 户口；`src/agf` 是 `knowledge.src` 下的未声明子目录。补了 `knowledge.agf`（整夹一张）并从父房间 exclude。presets/schemas/examples/tests 打整夹一张并写设计思路。普查从 never → current。

**最小任务**：冒烟补 `transfer` 预设（写存储、部分合同、不要 AG2K 地板）。`3559a62`。unittest 5 例 0.002s；verify 2 checker 共 0.2s。

**摩擦 / 缺陷（校准托管用）**：

1. MCP 默认 cwd 是 AG2C 自己，打 AGF 必须显式 `cwd`。漏了就会写错店。
2. PowerShell `$env:PYTHONPATH` 内联会被吃，必须 TEMP `.ps1`。
3. 未配监管：verify `regulator.outcome=unavailable` `not-configured`。AG2C 本体是 strict；外项目默认缺监管仍能 finish。
4. `ag2c task finish` 在仓库没有 `user.name`/`user.email` 时直接失败。enroll 不设 git 身份。已按 Initial commit 作者写了本地 config。
5. 冷启动期间 household/span 会改 governance，verify 要 `--full-scan`；只 census 当前房间会把其它房间打成 stale。
6. canary 理由中文在 PowerShell 输出里乱码。
7. finish 后仍 `undeclared-product`：外项目默认没有 scenario 检查。
8. first-drill 入学后两天都没人跑；本次 canary 由 `check.python` 拦住缺陷。
9. settle 不代录普查，census-review-required 会再摸市长。

**Backlog 建议**：enroll 后提示设 git identity；外项目监管缺省要明示（缺监管 vs 继承 AG2C 监管）；MCP 工具在非默认 cwd 时拒绝静默写错店。托管先覆盖 settle/census 认领，不要覆盖 finish 合并。

### 反向开发机制（摊平）——2026-09-11 与市长定稿

**起因**：单文件膨胀对 AI 维护者是双重税——胖文件废掉「文件名=语义摘要」的注意力路由，且每个碰它的会话都多烧 token（经济腿驱动因子）。实测分布：40 文件中 8 个 >800 行的文件占 57% 代码量（健康尾巴：28 个文件 <400 行）；**当日三个交互事故（amend×finish 死路→tasks.py 2160 行、census cwd 脚枪→mcp_server.py 1245 行、缓存 key 碰撞→households.py 926 行）全部发生在 >800 行的胖头**——文件超过单次注意力容量后交互 bug 滋生。

**现状盘点：治理语法全是正向的**

| 操作 | 现有机制 | 方向 |
|---|---|---|
| 加功能 | 任务回路全家桶 | 正向 ✅ |
| 移动文件 | `ag2c_rehome`（git mv + 全仓 import 重写 + 门禁） | 侧向 ✅ |
| 删除残留 | `retire` / `retire-confirm` | 反向，只管目录残骸 |
| 卡片清算 | 全量清算 | 反向，只管卡片不管代码 |
| **拆分/摊平代码** | **无** | **反向 ❌** |

房间预算是 GDP，文件软帽是基尼系数——只量总量时单文件可以在绿灯房间里癌变。预算体系的结构性盲区。

**机制四件套（detect → queue → operate → verify）**：

1. **发现**：src/ag2c 单文件 >800 行出警告（只警告不硬化，老规矩）——把盲区补成仪表。
2. **队列**：按「热度 × 肥胖度」排序；冷胖文件不碰不拆（boy scout：谁被任务碰到谁拆）。
3. **操作**：借 rehome 的 import 重写机器，加「提取集群 → 新模块 → 重写引用」split 原语。facade 先行：tasks.py 留 50 行 re-export，测试与 import 方不动，验证拆分后再清理。
4. **验收——纯搬运门**：摊平任务测试全绿是必要不充分；真正要回答「它只是搬运吗」。机器可验形式：**新增行必须全部来自删除行**（逐行哈希对账，豁免 import 行与模块 docstring），逻辑改动一行不许混入。比正向任务验收更硬更机械——摊平的正确性定义就是「什么都没变」。

**AGF 联动**：effect 维区分建设性 vs 消解性任务；坐标申报（fbf13b0 已落地）分类为消解类时自动套用纯搬运门——坐标系统第一次获得验收语义上的实际用途。

**tasks.py 接缝图**（首航病人，2160 行）：画像 lint 集群 ~150 行（514-638）／证据链集群 ~350 行（215-381 + 1997-2196）／start_task 160 行／verify_task 304 行／finish_task 132 行／orient 集群 ~180 行／记录层+worktree 助手 ~400 行。拆完每模块 150-350 行，落进健康桶。预检：集群间交叉引用密度分析防 import 环；`GOVERNANCE_CODE_PATHS` 同 diff 更新；`knowledge.ag2c-tasks` 卡拆 5-7 张。

**执行顺序**：软帽 ✅ → 机制 + tasks.py 首航 ✅ → 11 个胖文件摊平 ✅ `b21c8bd`（`src/` 没有超过 800 行）。后续谁再把文件养胖，谁用 `govern flatten-split` 拆。

### 开发成本治理——收敛设计（2026-09-11 与市长定稿）

- **三指标仪表**：高效（轮次/任务、任务时长、verify 失败率——今日实测均值 1.8 轮/任务、失败率 26%）｜经济（token，**显示单位 MTok**）｜有效（一次通过率、监管驳回率、交付后缺陷追溯）。
- **记账口径**：所有 API 的 token **等值聚合**，价格波动由市长脑内折算，系统不建价格表、不做钱折算。记录口径保真：`{model, input, output}` 字段照记，聚合才等值——后悔药留在数据里。
- **数据来源**：① 监管 API 的 usage 字段抠进 verify 记录（review.py，硬数据，零自报）；② finish 加可选自报字段（会话数/估算 token，标 INFERRED）；③ 高效/有效两腿全部从既有账本纯读。
- **铁律**：三条腿全部只警告不硬化（不进 ESCALATABLE_KINDS）；趋势棘轮阈值等数据攒够再定；按 AGF 坐标分层基线（「这种任务该花多少」）。
- **第一期切分**：监管 usage 落账 + `govern cost-report` 纯读仪表 + finish 自报字段 + 测试。出界：价格表、拦截、棘轮。

## 3. Backlog（优先级序）

| # | 事项 | 状态/依据 |
|---|---|---|
| 1 | ~~AGF 入学试飞~~ | ✅ `C:\WorkSpace\Code\agf`；首个产品提交 `3559a62`；摩擦见试飞报告 |
| 2 | ~~反向开发机制 + 11 个胖文件摊平~~ | ✅ `b21c8bd`；`src/` 没有超过 800 行（`imgui_tray.py` 792 最大） |
| 3 | 托管模式/市长代理层 | 决策分类学 ✅；**代理规则引擎不做**；抽查回火不开工 |
| 4 | ~~降档棘轮~~ | ✅ `91158e4` |
| 5 | enrollment/rehome 套件减重 | 数据支撑：两套件+ governance 占总验证耗时 40%（enrollment 8787s/rehome 6364s 累计） |
| 6 | tests 房间预算 | 约 15380/15500（`2203f1a` 去掉 unittest.main 样板后腾出空位）；仍不抬 15500 |
| 7 | 5.1 可信基仪式（+AG2K 离线签名锚） | 需市长设计对话，勿直接委托 |
| 8 | 6.1 政策沙盘（首议题：AG2K L2→L3 分拣标准） | 需市长设计对话，勿直接委托 |
| 9 | Scheduler Phase 2 | **已判决不做**（影子数据 0%），除非新证据 |
| 10 | MCP-tree 慢 | 未调查 |
| 11 | 监管 nit：warning-dismiss 先写历史后落账 | 低优 |

## 4. 操作手册（血泪版）

- **普查 cwd 脚枪**：MCP `ag2c_census` record=true 必须显式传 `cwd=<worktree 路径>`；不传默认 canonical，finish 不认（6d85939 后 canonical 录制会出 warning 字段，注意看）。
- **MCP server 版本偏斜**：server 是长驻进程，合并新代码后旧进程继续跑旧代码。症状：finish 报 census-stale 但普查明明是新的。处置：`cursor-agent mcp disable ag2c && cursor-agent mcp enable ag2c`，然后**重启 CLI 会话**（`cursor-agent --continue`）——disable/enable 只修配置层，活会话不会重连。CLI 兜底：verify/census 都可从 worktree 跑 `python -B -m ag2c ...`。
- **PowerShell 陷阱**：内联 `python -c "..."` 引号会被吃——驱动脚本写成 .py 放 `%TEMP%` 再跑；`>` 重定向产出 UTF-16LE（读出来是乱码/BOM），解析前先转码。
- **finish 窗口纪律**：census 新鲜（worktree 录、覆盖任务改动）+ verify 新鲜 + 其间无治理写/canonical 移动。canonical 动了 → `ag2c_task_refresh`（MCP，参数名是 `task`）→ 重录普查 → 重验 → finish。
- **全量验收门**：治理面变化触发全量时需 `task declare --full-scan --reason ...` 申报（落账），然后重验。
- **监管在线且严格**（DeepSeek，strict=true）：自证每条有副作用的声明必须引用本会话工具输出（命令+输出片段）；「测试过了」无输出=不交。
- **环境险情**：Windows 剪贴板是全局共享资源，任何软件持锁都会崩 gui 套件（见 backlog #1）。verify 卡 check.suite-gui 原生崩溃时先怀疑剪贴板，别怀疑代码。
- **并行会话**：多会话并行跑 verify 会互相踩负载（check.python 预算已按负载口径锚 35s）；发新委托任务前确认在飞任务数量。

## 5. 市长待决清单

1. tests 房间仍不抬 15500；再加测试先减重或挤进现有文件。
2. 其余胖文件拆分：boy scout，用 `ag2c govern flatten-split`，勿一轮拆完。
3. 5.1/6.1 设计对话的启动时机（AG2K 信任阶梯：L2→3 标准定宽变垃圾场、定严没人爬得上——准入宪法级权衡，须市长亲自主持）。
4. 成本治理趋势棘轮阈值：等第一期数据攒 2-4 周后定。
