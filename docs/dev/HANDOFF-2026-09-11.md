# 交接文档 2026-09-11（临时）

> **性质**：临时文档。归档条件：当下列「已决策的下一步」全部闭环、或本文档年龄超过两周且被新交接文档取代时，由继任任务删除或改写。接手方式：新会话先 `ag2c_task_orient`（MCP）拿开放任务队列，再读本文件，再查账本尾部事件。

## 1. 当前状态

- main @ `f62fb97`，与 origin 同步；v0.10.0 已发布（tag 在 `c2e4c5b`）。
- 今日合并（早→晚）：`0848c74` Scheduler Phase 1 影子模式 → `c2e4c5b` v0.10.0 → `535c53e` 动态预算警告永不硬化 → `fa07796` 治理代码对账 → `6d85939` 普查三修 → `99602b0` 验证通道健康 → `32fe45b` amend-portrait finish 侧死路修复 → `fbf13b0` AGF 坐标申报进 task start → `285ac0b` notify 空炮接线+危房推送 → `f62fb97` 坐标对账 checker。
- AGF 对账三件套：坐标申报 ✅（fbf13b0）／对账 ✅（f62fb97）／**降档棘轮 ⬜（未做）**。
- 审计四发现全部闭环：strict 401 泄漏（fa07796）、hazard 只拉不推（285ac0b）、notify 空炮（285ac0b）、patrol 只在 finish（托盘轮询即 watchdog）。
- 治理面：39 房间普查全 current；settle 清零；账本 3005 事件。
- 影子数据判决：精确 digest 缓存命中率 0%（16 次 verify），粗相关性回放可省 7.5% 且 0 漏失败——**Phase 2 引擎不做**，影子模式继续白跑攒数据。

## 2. 已决策的下一步（按序）

1. **剪贴板防御小任务（急，每个 verify 都在赌运气）**：imgui_bundle 对 `OpenClipboard` 失败不防御→原生崩溃→verify 卡死。已发作两次（QQ 持锁一次；第二次锁主未确证，微信时间线不符）。处方三段式：ctypes 预检 → 持锁则短窗口重试（第二次锁自行释放过，重试能赢）→ 超时仍锁才 skip 且**必须留痕**（醒目输出+可查记录，不做静默 skip）。画像核心：「任意进程持剪贴板锁时 verify 不再被崩死，且 skip 事件可见」。
2. **开发成本治理第一期（设计已收敛，见下）**。
3. **反向开发机制立项（重要，见下）**——治理语法目前全是正向的，「摊平代码」没有通道；机制与 tasks.py 首航互相验证。
4. **降档棘轮（三件套收官）**：坐标对账警告目前不计数不硬化；棘轮定义「持续宽松申报」的升级路径。等成本治理第一期落地后做，两者数据可互喂。

### 反向开发机制（摊平）——2026-09-11 与市长定稿

**起因**：单文件膨胀对 AI 维护者是双重税——胖文件废掉「文件名=语义摘要」的注意力路由，且每个碰它的会话都多烧 token（经济腿驱动因子）。实测分布：40 文件中 8 个 >800 行的文件占 57% 代码量（健康尾巴：28 个文件 <400 行）；**当日三个交互事故（amend×finish 死路→tasks.py 2160 行、census cwd 脚枪→mcp_server.py 1245 行、缓存 key 碰撞→households.py 926 行）全部发生在 >800 行的胖头**——文件超过单次注意力容量后交互 bug 滋生。

**现状盘点：治理语法全是正向的**

| 操作 | 现有机制 | 方向 |
|---|---|---|
| 加功能 | 任务回路全家桶 | 正向 ✅ |
| 移动文件 | `ag2c_rehome`（git mv + 全仓 import 重写 + 门禁） | 侧向 ✅ |
| 删除残留 | `retire` / `retire-confirm` | 反向，只管目录残骸 |
| 卡片清算 | 全量清算 | 反向，只管卡片不管代码 |
| **拆分/摊平代码** | **无** | **反向 ❌** |

房间预算是 GDP，文件软帽是基尼系数——只量总量时单文件可以在绿灯房间里癌变。预算体系的结构性盲区。

**机制四件套（detect → queue → operate → verify）**：

1. **发现**：src/ag2c 单文件 >800 行出警告（只警告不硬化，老规矩）——把盲区补成仪表。
2. **队列**：按「热度 × 肥胖度」排序；冷胖文件不碰不拆（boy scout：谁被任务碰到谁拆）。
3. **操作**：借 rehome 的 import 重写机器，加「提取集群 → 新模块 → 重写引用」split 原语。facade 先行：tasks.py 留 50 行 re-export，测试与 import 方不动，验证拆分后再清理。
4. **验收——纯搬运门**：摊平任务测试全绿是必要不充分；真正要回答「它只是搬运吗」。机器可验形式：**新增行必须全部来自删除行**（逐行哈希对账，豁免 import 行与模块 docstring），逻辑改动一行不许混入。比正向任务验收更硬更机械——摊平的正确性定义就是「什么都没变」。

**AGF 联动**：effect 维区分建设性 vs 消解性任务；坐标申报（fbf13b0 已落地）分类为消解类时自动套用纯搬运门——坐标系统第一次获得验收语义上的实际用途。

**tasks.py 接缝图**（首航病人，2160 行）：画像 lint 集群 ~150 行（514-638）／证据链集群 ~350 行（215-381 + 1997-2196）／start_task 160 行／verify_task 304 行／finish_task 132 行／orient 集群 ~180 行／记录层+worktree 助手 ~400 行。拆完每模块 150-350 行，落进健康桶。预检：集群间交叉引用密度分析防 import 环；`GOVERNANCE_CODE_PATHS` 同 diff 更新；`knowledge.ag2c-tasks` 卡拆 5-7 张。

**执行顺序**：软帽警告先行（纯测量）→ 机制 + tasks.py 首航（互相验证）→ 其余 7 个胖文件 boy scout。

### 开发成本治理——收敛设计（2026-09-11 与市长定稿）

- **三指标仪表**：高效（轮次/任务、任务时长、verify 失败率——今日实测均值 1.8 轮/任务、失败率 26%）｜经济（token，**显示单位 MTok**）｜有效（一次通过率、监管驳回率、交付后缺陷追溯）。
- **记账口径**：所有 API 的 token **等值聚合**，价格波动由市长脑内折算，系统不建价格表、不做钱折算。记录口径保真：`{model, input, output}` 字段照记，聚合才等值——后悔药留在数据里。
- **数据来源**：① 监管 API 的 usage 字段抠进 verify 记录（review.py，硬数据，零自报）；② finish 加可选自报字段（会话数/估算 token，标 INFERRED）；③ 高效/有效两腿全部从既有账本纯读。
- **铁律**：三条腿全部只警告不硬化（不进 ESCALATABLE_KINDS）；趋势棘轮阈值等数据攒够再定；按 AGF 坐标分层基线（「这种任务该花多少」）。
- **第一期切分**：监管 usage 落账 + `govern cost-report` 纯读仪表 + finish 自报字段 + 测试。出界：价格表、拦截、棘轮。

## 3. Backlog（优先级序）

| # | 事项 | 状态/依据 |
|---|---|---|
| 1 | 剪贴板防御 | 设计已定，提示词待写 |
| 2 | 成本治理第一期 | 设计已定（§2），提示词待写 |
| 3 | **反向开发机制立项，tasks.py 首航** | 设计已定（§2）；含文件软帽+split 原语+纯搬运门 |
| 4 | 降档棘轮 | 等 #2 落地 |
| 5 | enrollment/rehome 套件减重 | 数据支撑：两套件+ governance 占总验证耗时 40%（enrollment 8787s/rehome 6364s 累计） |
| 6 | tests 房间预算对话 | 14831/15500 行，96%——下次加测试即撞线 |
| 7 | MCP server 版本自检 | ag2c_mcp_health 报告加载代码版本/启动时间；普查记录已有 code_version 戳（6d85939），对账原料已备 |
| 8 | 5.1 可信基仪式（+AG2K 离线签名锚） | 需市长设计对话，勿直接委托 |
| 9 | 6.1 政策沙盘（首议题：AG2K L2→L3 分拣标准） | 需市长设计对话，勿直接委托 |
| 10 | Scheduler Phase 2 | **已判决不做**（影子数据 0%），除非新证据 |
| 11 | gui segfault | 根因已查明：外部进程持剪贴板锁+imgui_bundle 不防御；#1 即修复 |
| 12 | MCP-tree 慢 | 未调查 |
| 13 | 监管 nit：warning-dismiss 先写历史后落账 | 低优 |

## 4. 操作手册（血泪版）

- **普查 cwd 脚枪**：MCP `ag2c_census` record=true 必须显式传 `cwd=<worktree 路径>`；不传默认 canonical，finish 不认（6d85939 后 canonical 录制会出 warning 字段，注意看）。
- **MCP server 版本偏斜**：server 是长驻进程，合并新代码后旧进程继续跑旧代码。症状：finish 报 census-stale 但普查明明是新的。处置：`cursor-agent mcp disable ag2c && cursor-agent mcp enable ag2c`，然后**重启 CLI 会话**（`cursor-agent --continue`）——disable/enable 只修配置层，活会话不会重连。CLI 兜底：verify/census 都可从 worktree 跑 `python -B -m ag2c ...`。
- **PowerShell 陷阱**：内联 `python -c "..."` 引号会被吃——驱动脚本写成 .py 放 `%TEMP%` 再跑；`>` 重定向产出 UTF-16LE（读出来是乱码/BOM），解析前先转码。
- **finish 窗口纪律**：census 新鲜（worktree 录、覆盖任务改动）+ verify 新鲜 + 其间无治理写/canonical 移动。canonical 动了 → `ag2c_task_refresh`（MCP，参数名是 `task`）→ 重录普查 → 重验 → finish。
- **全量验收门**：治理面变化触发全量时需 `task declare --full-scan --reason ...` 申报（落账），然后重验。
- **监管在线且严格**（DeepSeek，strict=true）：自证每条有副作用的声明必须引用本会话工具输出（命令+输出片段）；「测试过了」无输出=不交。
- **环境险情**：Windows 剪贴板是全局共享资源，任何软件持锁都会崩 gui 套件（见 backlog #1）。verify 卡 check.suite-gui 原生崩溃时先怀疑剪贴板，别怀疑代码。
- **并行会话**：多会话并行跑 verify 会互相踩负载（check.python 预算已按负载口径锚 35s）；发新委托任务前确认在飞任务数量。

## 5. 市长待决清单

1. tests 房间撞线时：减重（backlog #4）还是提预算——建议减重，数据在 §3。
2. tasks.py 拆分时机（backlog #6）。
3. 5.1/6.1 设计对话的启动时机（AG2K 信任阶梯：L2→3 标准定宽变垃圾场、定严没人爬得上——准入宪法级权衡，须市长亲自主持）。
4. 成本治理趋势棘轮阈值：等第一期数据攒 2-4 周后定。
