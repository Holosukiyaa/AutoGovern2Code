[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "autogovern2code"
version = "0.3.0"
description = "Zero-touch, evidence-backed governance for AI coding changes"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [
  { name = "AutoGovern2Code contributors" }
]
keywords = ["governance", "developer-tools", "ai-agents", "codex", "git-worktree", "verification"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Software Development :: Quality Assurance",
]
dependencies = []

[project.urls]
Documentation = "https://github.com/Holosukiyaa/AutoGovern2Code#readme"
Issues = "https://github.com/Holosukiyaa/AutoGovern2Code/issues"
Source = "https://github.com/Holosukiyaa/AutoGovern2Code"

[project.scripts]
ag2c = "ag2c.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
ag2c = ["py.typed", "skills/*/SKILL.md", "skills/*/agents/*.yaml"]
